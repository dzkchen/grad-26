# The api/ directory is the Go service deployed to Cloud Run, NOT Vercel
# Serverless Functions. Without this, Vercel's zero-config detector picks up
# api/cmd/server/main.go and fails to build because it expects an exported
# Vercel Go handler.
api/
